origin    : Test-Requires
name      : perl-test-requires
version   : 0.10
category  : perl
summary   : Test::Requires - Checks to see if the module can be loaded
maintainer: devel@slitaz.org
license   : GPL
home      : https://metacpan.org/release/Test-Requires
target    : noarch
repology  : perl:test-requires

src:
- url:  cpan://TOKUHIROM/<o>-<v>.tar.gz

bdeps: perl

make:
  type: perl

pkgs:
- pkg : main
  deps: perl
